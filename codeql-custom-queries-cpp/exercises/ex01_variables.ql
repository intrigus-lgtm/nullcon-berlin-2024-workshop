import cpp

/*
 * Find all variables named "basedir" of type `std::string`.
 * Hints:
 * - Get the type of a variable using the `getType()` method.
 * - Use `hasQualifiedName(namespace, type)` to check where a type is defined.
 */

from Variable v
// TODO: add where clause that solves the exercise
select v
